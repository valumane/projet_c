--Projet pour la matiere Programmation C avancé--

Le but etait de crée un pipeline de processus worker,
pour simuler le crible de hoare

utilisation : 
dans un terminal vous lancé ./master
dans un deuxieme terminal vous lancé ./client 
./client compute <n> -> vous renvoie si n est premier ou non
./client howmany -> renvoie combien de nombre premier on etait trouvé
./client highest -> renvoie le plus grand nombre premier trouvé
./client local <n> -> comme compute, mais sauf qu'au lieu d'utilisé un pipeline de processus, il le fait avec des thread
./client stop -> stop tout

compilation : 
Pour compiler :
- "make" (ou "make all") : compile tout le projet
- "make <exe>" : compile un seul binaire
     donc il y a 3 possibilités : "make master", "make worker", "make client"
- "make <fichier.o>" : compile un seul .c
     par exemple "make master_client.o" pour compiler master_client.c
- "make clean" : supprime les .o et les .d
- "make distclean" : supprime les .o, les .d et les exécutables
- le Makefile est à adapter si vous ajoutez des fichiers source

Suppression des tubes nommés et des sémaphore :
- le script rmsempipe.sh est fourni
- il supprime tous les sémaphores avec les droits 641
- il supprime les deux tubes nommés avec des noms précisés en dur
- le script est à adapter pour mettre les noms des tubes que vous avez choisis
